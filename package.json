ch{
  "name": "rmt",
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate.js",
    "verify": "./verifier.sh"
  },
  "dependencies": {
    "ethers": "^6.13.2",
    "canonicalize": "^1.0.8"
  }
}
